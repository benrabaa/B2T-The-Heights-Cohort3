var input =prompt("Enter name"); //Assign  answer of the prompt screen to variable(INPUT)
console.log(input);
			
document.getElementById("MyName").innerHTML=input;  //Assign variable(INPUT) vale to element id MyName(H1) which is the answer from prompt